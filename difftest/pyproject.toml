[project]
name = "difftest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "greenlamp",
    "matplotlib>=3.10.5",
    "twine>=6.1.0",
]

[tool.uv.sources]
greenlamp = { path = "../greenlamp", editable = true }

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "jupyter>=1.1.1",
]
